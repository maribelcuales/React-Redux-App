const initialState = {
  films: [],
  isFetching: false,
  error: ""
}; 

export const filmsReducer = (state = initialState, action) => {
  switch (action.type) {
    default: 
      return state;
  }
};
